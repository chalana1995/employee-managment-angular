import { LkrformatterPipe } from './lkrformatter.pipe';

describe('LkrformatterPipe', () => {
  it('create an instance', () => {
    const pipe = new LkrformatterPipe();
    expect(pipe).toBeTruthy();
  });
});
